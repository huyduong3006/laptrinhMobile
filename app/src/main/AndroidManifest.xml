<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.quanlydatve_sqlite">
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:usesCleartextTraffic="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:requestLegacyExternalStorage="true"
        android:theme="@style/Theme.QuanLyDatVe_SQLite"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">
        <activity
            android:name=".activities.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>

        <activity android:name=".activities.RegisterActivity" />
        <activity android:name=".activities.MainActivity" />
        <activity android:name=".activities.TraCuuChuyenBayActivity" />
        <activity android:name=".activities.DatVeActivity" />
        <activity android:name=".activities.ThanhToanActivity" />

        <activity android:name=".activities.ProfileActivity" />
        <activity android:name=".activities.HoaDonBooking" />
        <activity android:name=".activities.LichSuDatVeActivity" />

        <activity android:name=".activities.DanhSachChuyenBayDieuKienActivity" />

        <activity android:name=".activities.AdminQuanLyChuyenBay" />

        <activity android:name=".activities.EditUserActivity" />

        <activity android:name=".activities.AddChuyenBayActivity" />

        <activity android:name=".activities.EditChuyenBayActivity" />

    </application>

</manifest>